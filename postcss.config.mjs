// Tailwind CSS v4 — single PostCSS plugin, no config file needed.
// ARCHITECTURE.md §2: "Tailwind CSS v4 — v4 native CSS engine, không cần
// PostCSS pipeline phức tạp."
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
