export default function SchedulesPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Schedules</h1>
      <p className="mt-2 text-sm text-[hsl(var(--foreground))]/60">
        Recurring schedule list lands in Phase 2 (read-only stub for now).
      </p>
    </div>
  );
}
