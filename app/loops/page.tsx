export default function LoopsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Loops</h1>
      <p className="mt-2 text-sm text-[hsl(var(--foreground))]/60">
        Goal-loop list lands in Phase 2 (read-only stub for now).
      </p>
    </div>
  );
}
